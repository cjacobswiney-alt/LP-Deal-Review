import React from 'react'
import ReactDOM from 'react-dom/client'
import AnalyzerApp from '../lp-pitch-analyzer.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AnalyzerApp />
  </React.StrictMode>,
)
